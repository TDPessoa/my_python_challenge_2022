# Imports necessários:
import random

# Defs

    # Ações:
        
        def atk() :
            
            e_hp -= h_dmg
        
        def blk() :
            
            if ( e_act == 1 ) :
                h_hp -= e_dmg - h_blk_vlu
                print(f'Você defendeu o ataque do {enemy},
                reduzindo o dano de {e_dmg} para {e_dmg-h_blk_vlu}.'
            
            elif (e_act == 2 ) :
                print(f'O {enemy} também defendeu') 
    # Feitiços
   
    # Elites

    # Inimigos

# Cinco inimigos e um elite

for c in range ( 4 ) :
    